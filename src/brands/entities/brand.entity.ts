// Entidad creada para definir el objeto Brand (una entidad se asociará directamente con una tabla de una base de datos)

export class Brand {

    id: string;
    name: string;
    createdAt: number;
    updateAt?: number;
}
