// Interfaz creada para definir el objeto Car (una interfaz sólo contiene la definición de métodos y propiedades, no su implementación)

export interface Car {
    id: string;
    model: string;
    brand: string;
}
